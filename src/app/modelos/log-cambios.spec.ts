import { async, ComponentFixture, TestBed } from '@angular/core/testing';
import { IonicModule } from '@ionic/angular';

import { LogCambios } from './log-cambios';

describe('LogCambios', () => {
  it('should create an instance', () => {
    expect(new LogCambios()).toBeTruthy();

  });
});
